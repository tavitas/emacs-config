#include <cs50.h>
#include <stdio.h>
#include <ctype.h>
#include <strings.h>

int main(int argc, string argv[]) {
	if (argc == 2){
		printf("plaintext: ");
		string p = get_string();

		int k = atoi();

		for (int i = 0, l = strlen(p);i < l;i++) {
			
		}
}
